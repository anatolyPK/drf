#version: '3'

services:
  web:
    build:
      context: .
    ports:
      - '8000:8000'
    restart: always
    container_name: 'demo_app_django'
    volumes:
      - .:/src
    command: >
      sh -c "python src/manage.py runserver 0.0.0.0:8000"
#  redis:
#    image: redis
#    restart: always
#    container_name: 'demo_app_redis'
#    command: redis-server
#    ports:
#      - '6379:6379'
#  celery:
#    build: .
#    container_name: 'demo_app_celery'
#    command: pipenv run celery -A config worker -B
#    volumes:
#      - .:/code
#    links:
#      - redis
#    depends_on:
#      - web
#      - redis